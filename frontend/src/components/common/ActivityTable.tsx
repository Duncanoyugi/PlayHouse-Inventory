import type React from 'react'
import { RefreshCw } from 'lucide-react'
import { Button } from './Buttons/Button'

export function ActivityTable({ title, description, rows, columns, onRefresh, emptyMessage = 'No activity found.' }: { title: string; description: string; rows: any[]; columns: { key: string; label: string; render?: (row: any) => React.ReactNode }[]; onRefresh?: () => void; emptyMessage?: string }) {
  return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-200 p-4"><div><h2 className="font-semibold text-slate-900">{title}</h2><p className="text-sm text-slate-500">{description}</p></div>{onRefresh && <Button size="sm" variant="outline" onClick={onRefresh}><RefreshCw className="mr-2 h-4 w-4" /> Refresh</Button>}</div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{columns.map((column) => <th key={column.key} className="px-4 py-3">{column.label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row, index) => <tr key={row.id || index} className="hover:bg-slate-50">{columns.map((column) => <td key={column.key} className="whitespace-nowrap px-4 py-4 text-slate-600">{column.render ? column.render(row) : row[column.key] || '-'}</td>)}</tr>)}{!rows.length && <tr><td colSpan={columns.length} className="px-4 py-12 text-center text-slate-500">{emptyMessage}</td></tr>}</tbody></table></div></section>
}
