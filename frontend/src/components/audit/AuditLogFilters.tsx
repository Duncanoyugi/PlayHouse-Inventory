export const AuditLogFilters = (_props: { filters: any; onFilterChange: (f: any) => void }) => {
  return (
    <div className="bg-white p-4 rounded-xl shadow-sm border border-gray-200">
      <p className="text-gray-500">Audit log filters coming soon.</p>
    </div>
  )
}
