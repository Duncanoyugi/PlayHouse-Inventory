export const InventoryFilters = (_props: { filters: any; onFilterChange: (f: any) => void; locations: any[] }) => {
  return (
    <div className="bg-white p-4 rounded-xl shadow-sm border border-gray-200">
      <p className="text-gray-500">Inventory filters coming soon.</p>
    </div>
  )
}
